/* v1 legacy file — replaced in v2 by the store layer.
 * See store/index.js (selector), store/local.js (JSON demo mode)
 * and store/supabase.js (production). Safe to delete. */
module.exports = require("./store");
